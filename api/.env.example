SUPABASE_URL=https://zbypsyftfumjsvzbrefu.supabase.co
SUPABASE_SERVICE_KEY=   # service_role — Supabase dashboard > Settings > API
GROQ_API_KEY=           # Groq dashboard — rotate if previously exposed in code
FRONTEND_URL=http://localhost:5173
PORT=3001

# Microsoft Graph — Outlook Calendar sync
MS_TENANT_ID=           # Azure portal > App registrations > Overview > Directory (tenant) ID
MS_CLIENT_ID=           # Azure portal > App registrations > Overview > Application (client) ID
MS_CLIENT_SECRET=       # Azure portal > App registrations > Certificates & secrets > Value
MS_CALENDAR_USER=       # Email del usuario M365 cuyo calendario se usará (ej. admin@empresa.com)
